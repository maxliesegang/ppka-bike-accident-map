# Prepared Unfallatlas data

This directory contains the yearly CSV files served by the app. Files with a four-digit year in their name are discovered automatically and appear in the year filter.

Generate filtered files from `data/unfallatlas-raw` with:

```bash
npm run unfallatlas:extract:bw
```
